import { colors } from "../theme/colors";
import { spacing } from "./spacing";
import { fontSize, fontWeight } from "./typograpghy";

export const theme = {
  colors,
  spacing,
  fontSize,
  fontWeight,
};